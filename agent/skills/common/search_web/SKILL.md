# Skill: Search Web

Use when:
- The user asks for external information not present in the workspace.

Rules:
- Search only when the task genuinely needs outside information.
- Summarize findings briefly and tie them back to the task.
- Avoid search if local observation or memory already answers the need.
